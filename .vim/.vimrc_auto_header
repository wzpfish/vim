"新建.c,.h,.sh,.java文件，自动插入文件头 
autocmd BufNewFile *.py,*.org exec ":call SetTitle()" 
""定义函数SetTitle，自动插入文件头 
func SetTitle() 
    "如果文件类型为python文件 
    if &filetype == 'python' 
        call setline(1, "# coding: utf-8") 
    else
        call setline(1,"\#+TITLE: Insert Your Own Title") 
        call append(line("."), "\#+AUTHOR: wzpfish") 
        call setline(3, "\#+LATEX_HEADER: \usepackage{xeCJK}")
    endif
    "新建文件后，自动定位到文件末尾
    autocmd BufNewFile * normal G
endfunc 

